import { Fragment } from "react"

const Header = () => (
    <section class="hero is-primary">
        <div class="hero-body">
            <p class="title">
                Here is the Header!
            </p>
        </div>
    </section>
)

export default Header