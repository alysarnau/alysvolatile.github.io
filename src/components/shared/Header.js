import { Fragment } from "react"

const Header = () => (
    <Fragment class="hero">
        <div class="hero-body">
            <p class="title">
                Here is the Header!
            </p>
        </div>
    </Fragment>
)

export default Header